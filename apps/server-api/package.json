{
  "name": "@dream-invoice/server-api",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "tsx --test test/*.test.ts",
    "build": "pnpm typecheck",
    "check": "pnpm typecheck && pnpm test",
    "lint": "eslint ."
  },
  "dependencies": {
    "@dream-invoice/licensing": "workspace:*",
    "@dream-invoice/server-core": "workspace:*"
  },
  "devDependencies": {
    "@types/node": "^20",
    "tsx": "^4.22.4",
    "typescript": "latest"
  }
}
