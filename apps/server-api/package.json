{
  "name": "@invoice-platform/server-api",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "tsx --test test/*.test.ts",
    "build": "pnpm typecheck",
    "check": "pnpm typecheck && pnpm test"
  },
  "dependencies": {
    "@invoice-platform/licensing": "workspace:*",
    "@invoice-platform/server-core": "workspace:*"
  },
  "devDependencies": {
    "@types/node": "^20",
    "tsx": "latest",
    "typescript": "latest"
  }
}
